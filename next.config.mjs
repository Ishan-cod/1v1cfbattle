/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["userpic.codeforces.org"],
  },
  reactCompiler: true,
};

export default nextConfig;
